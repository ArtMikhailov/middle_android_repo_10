К сожалению не успел все проверить и исправить, но на 10 ошибок должно набраться. Для корректной работы приложения нужно добавить WEATHER_API_KEY в location.properties.

Комментарии к части найденных и исправленных ошибок:

1. В файле манифеста присутствует атрибут (android:usesCleartextTraffic="true") позволяющий использование небезопасных протоколов без шифрования, например http вместо https, что является угрозой безопасности (OWASP M8 Security Misconfiguration)

Метод: Сбор информации. Анализ файла AndroidManifest.xml

Исправление: удалил атрибут

2. В файле app/build.gradle.kts выключена обфускация кода, что является угрозой безопасности (OWASP M7 Insufficient Binary Protections)

Метод: Сбор информации. Изучение файлов build.gradle

Исправление: включил обфускацию кода.

3. В интерфейсе WeatherApiService в коде присутствует api key для работы с open weather api, что является угрозой безопасности (OWASP M1 Improper Credential Usage)

Метод: Статический анализ кода на наличие жестко закодированных учетных данных.

Исправление: перенс api key в local.properties. Сделал запись ключа из local.properties в BuildConfig в процессе сборки.

4. В интерфейса WeatherApiService BASE_URL для запросов к open weather api определен через небезопасный протокол http, что является угрозой безопасности (OWASP M5 Insecure Communication)

Метод: Статический анализ кода на использование небезопасных протоколов передачи данных.

Исправление: заменил протокол на https

5. В классе WeatherApplication присутствует утечка памяти: сохранение контекста в статическое поле.

Метод: статический анализ кода.

Исправление: удалил сохранение контекста в статическое поле.

6. Класс ImageLoader содержит ряд проблем:
- утечки памяти (ссылки на ImageView, которые никак не очищаются, статическая ссылка на application context).
- Хранение битмапов в памяти без каких либо ограничений и механизма очищения кэша, они могут занимать много памяти.
- Предназначен для работы с ImageView, хотя интерфейсы написаны на Compose
- Сырая реализация механизма загрузки и кэширования изображений
- Метод: статический анализ кода

Исправление: удалил класс, перевел работу с изображениями на уже продуманную и проверенную библиотеку Coil.

7. Класс LocationTracker содержит ряд проблем:

- утечка памяти: являясь синглтоном, он держит ссылку на контекст
- в проекте уже есть класс LocationRepository. Логику получения локации и получения обновлений локации разумно держать в одном классе.

Метод: статический анализ кода

Исправление: удалил класс.

8. В MainActivity вью модель инициализируется напрямую. Что влечет за собой некорректную работу вью модели (не будет переживать смену конфигурации, onCleared не будет вызван, и др.)

Метод: статический анализ кода

Исправление: сделал получение вью модели через функцию viewModels

9. В MainActivity не достаточно корректная работа с разрешением на локацию:

- не хватает показа сообщения с разъясненем для чего нужно разрешение
- события получения и отклонения разрешения никак не учитываются в логике приложения

Метод: тестирование бизнес логики и статический анализ кода

Исправление: скорректировал работу с разрешением на локацию

10. WeatherViewModel производит многочисленные баги в работе приложения.

Метод: тестирование бизнес логики и статический анализ кода

Исправление: Исправил баги путем

- четкого разделения логики получения погоды по локации и по городу,
- выделения UiState вместо разрозненных полей состояний,
- перехода на suspend функции, корутины и StateFlow
- обработки событий получения и отклонения разрешения на локацию



К другим найденным и исправленным ошибкам оставил комментарии в коде.